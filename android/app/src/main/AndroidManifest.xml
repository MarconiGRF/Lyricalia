<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/lyircon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/lyircon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lyricalia"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activities.SpotifyLinkActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:host="link" android:scheme="lyricalia" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MenuActivity"
            android:exported="true"/>
        <activity
            android:name=".activities.match.MatchCreateActivity"
            android:exported="true"/>
        <activity
            android:name=".activities.match.MatchWaitingActivity"
            android:exported="true"/>
        <activity
            android:name=".activities.match.MatchJoinActivity"
            android:windowSoftInputMode="stateVisible"
            android:exported="true"/>
        <activity
            android:name=".activities.match.MatchOngoingActivity"
            android:windowSoftInputMode="stateVisible"
            android:exported="true"/>
    </application>

</manifest>